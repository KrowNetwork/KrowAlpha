query getAllUsers {
    description: "seturn every user in the system"
    statement:
        SELECT org.krow.model.User
}
query whereUserWorks {
    description: "select all jobs based on their User"
    statement: 
        SELECT org.krow.model.Job
            WHERE (user == _$user)
}
query getAllHires{
  description: "return all hires"
  statement: SELECT org.krow.model.Hire
}